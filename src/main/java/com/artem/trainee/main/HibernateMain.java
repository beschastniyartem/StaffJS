package com.artem.trainee.main;

/**
 * Created on 09.07.14.
 */
public class HibernateMain {
    public static void main(String[] args) {

/*            Department department = new Department();
            department.setName("David");


            //Get Session
            Session session = HibernateUtil.getSessionFactory().openSession();
            //start transaction
            session.beginTransaction();
            //Save the Model object
             Department departmentToUpdate = (Department) session.get(Department.class, 79);
             departmentToUpdate.setName("LALA2310");
             System.out.println("DTU = " + departmentToUpdate);
             session.save(departmentToUpdate);
         //   session.update(department);
            //Commit transaction
            session.getTransaction().commit();
            System.out.println("Department ID=" + departmentToUpdate.getDep_id());

            //terminate session factory, otherwise program won't end
            session.close();
       *//* departmentToUpdate.setName("Ivan");
           Session session1 =HibernateUtil.getSessionFactory().openSession();
            session1.save(departmentToUpdate);
            session1.close();*//*
        System.out.println("THE END");*/
    }
}